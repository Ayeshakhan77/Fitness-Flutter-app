name: fitness_app_backend
description: A backend for the fitness application.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  mysql1: ^0.20.0
  crypto: ^3.0.3
  dart_jsonwebtoken: ^2.7.1
  shelf_cors_headers: ^0.1.5

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
